# Java 基础 面试题

> 生成时间：2026-07-30
> 请在每题 `### A：` 下方填写你的回答，完成后回复「完成」。

---

## Q1：`String`、`StringBuilder`、`StringBuffer` 三者有什么区别？为什么 Java 要把 `String` 设计成不可变（immutable）的？不可变带来了哪些好处？

### A：

（在此填写你的回答）

### evaluation

（AI 评分区域，用户无需填写）

---

## Q2：请分别说明 `final`、`finally`、`finalize` 的作用。它们之间有任何关系吗？`finally` 块在哪些情况下不会被执行？

### A：

（在此填写你的回答）

### evaluation

（AI 评分区域，用户无需填写）

---

## Q3：请说明 Java 泛型的实现机制。什么是「类型擦除（Type Erasure）」？类型擦除会带来哪些限制和坑？`List<String>` 和 `List<Integer>` 的 `class` 是否相等，为什么？

### A：

（在此填写你的回答）

### evaluation

（AI 评分区域，用户无需填写）

---

## Q4：`synchronized` 和 `volatile` 的区别是什么？各自适用于什么场景？请详细说明 `synchronized` 在 JDK 1.6 之后的「锁升级」过程（偏向锁 -> 轻量级锁 -> 重量级锁）。

### A：

（在此填写你的回答）

### evaluation

（AI 评分区域，用户无需填写）

---

## Q5：请说明 Java 的类加载机制与「双亲委派模型」。双亲委派的作用是什么？请举出至少两种打破双亲委派模型的典型场景，并说明其原理。

### A：

（在此填写你的回答）

### evaluation

（AI 评分区域，用户无需填写）

---

## Q6：某线上服务接口响应时间突然变慢，经初步排查怀疑是频繁 Full GC 导致。请描述你的完整排查思路、使用的工具，以及常见引发 Full GC 的原因有哪些？

### A：

（在此填写你的回答）

### evaluation

（AI 评分区域，用户无需填写）

---

## Q7：在项目中需要实现一个「线程安全的单例模式」。请给出至少两种实现方式（如饿汉式、双重检查锁、静态内部类、枚举等），并对比分析它们的优缺点、线程安全性和延迟加载特性。

### A：

（在此填写你的回答）

### evaluation

（AI 评分区域，用户无需填写）

---

## Q8：谈谈你对 Java 内存模型（JMM）的理解。`volatile` 是如何保证「可见性」和「有序性」的？「happens-before」原则在实践中有什么意义？请举例说明。

### A：

（在此填写你的回答）

### evaluation

（AI 评分区域，用户无需填写）

---

## 总成绩

（AI 评分完成后填写：X / 100，及整体评价与改进建议）
