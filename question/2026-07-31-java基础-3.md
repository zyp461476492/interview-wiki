# Java 基础 面试题

> 生成时间：2026-07-31
> 请在每题 `### A：` 下方填写你的回答，完成后回复「完成」。
> 参考答案已随生成登记进 wiki（话题页 `#### 参考答案`），本文件不含答案；评分时由 AI 从 wiki 取回填入下方 evaluation。

---

## Q1：**`ArrayList` 和 `LinkedList` 有什么区别？请从底层数据结构、增删改查性能、内存占用、应用场景等方面进行对比。为什么说 `ArrayList` 的随机访问是 O(1) 而 `LinkedList` 是 O(n)？**

### A：

（在此填写你的回答）

### evaluation

（AI 评分区域，用户无需填写；AI 参考答案优先取自 wiki）

---

## Q2：**Java 是「值传递」还是「引用传递」？请结合代码示例说明：当一个对象作为参数传入方法后，方法内部修改该对象的属性，外部是否可见？如果方法内部让该参数引用指向一个新对象，外部会受影响吗？为什么？**

### A：

（在此填写你的回答）

### evaluation

（AI 评分区域，用户无需填写；AI 参考答案优先取自 wiki）

---

## Q3：**什么是 Java 的反射机制？它能在运行时做哪些事情？反射有哪些典型应用场景？反射的性能问题主要出在哪里，有哪些优化手段？**

### A：

（在此填写你的回答）

### evaluation

（AI 评分区域，用户无需填写；AI 参考答案优先取自 wiki）

---

## Q4：**什么是 Java 对象的序列化与反序列化？`Serializable` 接口的作用是什么？`serialVersionUID` 有什么用？请说明 `transient` 关键字的作用，以及序列化可能带来的安全问题。**

### A：

（在此填写你的回答）

### evaluation

（AI 评分区域，用户无需填写；AI 参考答案优先取自 wiki）

---

## Q5：**请解释 Java 中「深拷贝（Deep Copy）」和「浅拷贝（Shallow Copy）」的区别。如何实现深拷贝？请至少给出两种实现方式并对比其优缺点。**

### A：

（在此填写你的回答）

### evaluation

（AI 评分区域，用户无需填写；AI 参考答案优先取自 wiki）

---

## Q6：**请说明 Java 8 引入的 `Stream API` 和 `Lambda` 表达式。`map`、`filter`、`reduce`、`collect` 等操作的区别是什么？什么是「短路操作」？请说明 Stream 的「懒加载（lazy）」特性是如何提升性能的。**

### A：

（在此填写你的回答）

### evaluation

（AI 评分区域，用户无需填写；AI 参考答案优先取自 wiki）

---

## Q7：**请说明 `ThreadLocal` 的作用与实现原理。为什么 `ThreadLocal` 可能导致内存泄漏？应如何正确使用与清理？`InheritableThreadLocal` 与 `ThreadLocal` 有何区别？**

### A：

（在此填写你的回答）

### evaluation

（AI 评分区域，用户无需填写；AI 参考答案优先取自 wiki）

---

## Q8：**Java 中创建对象有哪几种方式？请分别说明 `new`、反射、`clone()`、反序列化、`Unsafe.allocateInstance()` 等方式的区别，以及它们是否调用构造方法。**

### A：

（在此填写你的回答）

### evaluation

（AI 评分区域，用户无需填写；AI 参考答案优先取自 wiki）

---

## Q9：**Java 中的内部类有哪几种（静态内部类、成员内部类、局部内部类、匿名内部类）？它们各自的特点与使用场景是什么？为什么匿名内部类只能访问 `final`（或 effectively final）的局部变量？**

### A：

（在此填写你的回答）

### evaluation

（AI 评分区域，用户无需填写；AI 参考答案优先取自 wiki）

---

## Q10：**请说明 Java 注解（Annotation）的工作机制。常见的元注解（`@Retention`、`@Target`、`@Inherited`、`@Documented`、`@Repeatable`）分别是什么含义？如何自定义一个注解并在运行时通过反射读取它？**

### A：

（在此填写你的回答）

### evaluation

（AI 评分区域，用户无需填写；AI 参考答案优先取自 wiki）

---

## 总成绩

（AI 评分完成后填写：X / 100，及整体评价与改进建议）
